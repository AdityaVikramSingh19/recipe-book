import React from 'react';

const AddRecipe = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h2>Add a New Recipe</h2>
      <p>This is where the form to add a new recipe will go.</p>
    </div>
  );
};

export default AddRecipe;
